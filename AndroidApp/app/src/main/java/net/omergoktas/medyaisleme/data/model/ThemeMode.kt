package net.omergoktas.medyaisleme.data.model

enum class ThemeMode(val title: String) {
    SYSTEM("Sistem Varsayılanı"),
    LIGHT("Açık Tema (Light Mode)"),
    DARK("Koyu Tema (Dark Mode)"),
    DYNAMIC("Dinamik Renkler (Material You / Monet)")
}
